# ============================================
# DTrader-5 Development Environment
# ============================================

environment: development

# Override для development
redis:
  host: localhost
  port: 6379

exchange:
  testnet: true

logging:
  level: debug
  format: pretty

trading:
  mode: paper

monitoring:
  enabled: false
